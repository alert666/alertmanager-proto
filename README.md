# alertmanager-proto

```bash
brew install buf                                  # 安装 buf
buf dep update                                    # 拉取依赖（可选）
buf generate                                      # 根据 buf.gen.yaml 生成 pb 代码
```
